#include<stdio.h>
#define MAX 1000
int main(){
    int i = 100;
    int *ip =  &i;
     
    //ip is the location in memory that store i
    printf("i is %d, and the pointer(location) is %p\n",i,ip); 
    //*ip is the value of the varaible that been stored into location ip, which is i
    printf("the location %p, store a variable which is %d\n",ip,*ip);
    
    #include <stdio.h>

    int x;

    printf("memory address of x in hexadecimal is %p\n", &x);

    
    return 0;
}
