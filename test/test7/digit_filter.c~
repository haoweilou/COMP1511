//Haowei Lou
//z5258575
//
#include<stdio.h>
int main(void){
    int ch = getchar();
    while(ch != EOF){
        if((ch >= '0' && ch <= '9') || ch == ' ' || ch == '\n'){
            putchar(ch);
        }else{
            putchar('.');
        }
        ch = getchar();
    }
    return 0;
}
