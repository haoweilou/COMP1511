#include<stdio.h>
#include<string.h>
void increment(int *x){
    *x=*x+1;
}
int main(){
    int i = 13;
    while(i<20){
        printf("%d\n", i);
        increment(&i);
    }
    int j = 47;

    
    printf("i=%d j=%d\n", i, j);
    swap(i, j);
    printf("i=%d j=%d\n",i, j);
    
    return 0;
}
