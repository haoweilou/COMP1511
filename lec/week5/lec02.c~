#include<stdio.h>
#define LENGTH 14
int main(void){
    int ch = getchar();
    while(ch!=EOF){
        printf("You entered the character: '%c' which has ASCII code %d\n",ch,ch);
        ch=getchar();
    }
        return 0;
}
